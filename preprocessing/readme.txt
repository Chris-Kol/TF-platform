drug_sigs_for_cell.R

Cleans the perturbations for each cell line from replicate compounds at different time points and dosages.
Perturbations with the highest quality are kept.

get_cmap_signatures.R

Given signature IDs the function loads the z-scores for the signatures, user can select if to only keep the landmark genes.

sh_rna_cgs_signatures_forTFs.R <- Cleans the signatures of consensus shRNA knockdowns for the TFs whose enrichment can be calculated with VIPER

